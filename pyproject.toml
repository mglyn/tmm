[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tmm-chart"
version = "0.1.0"
description = "Experiment pipeline for 2D curriculum chart understanding"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
